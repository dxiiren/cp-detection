/**
 * Injected by Vite `define` in `vite.config.ts`, so the server render and the
 * client bundle are handed the exact same literal and there is no chance of a
 * canonical URL that differs between the two.
 */
declare const __SITE_URL__: string
declare const __SITE_INDEXABLE__: boolean
